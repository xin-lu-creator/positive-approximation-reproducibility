# Metadata policy

`CITATION.cff` is the machine-readable metadata source for GitHub/Zenodo integration. A `.zenodo.json` file is intentionally not included because Zenodo gives `.zenodo.json` precedence and would ignore `CITATION.cff` if both were present.

Do not add an unverified ORCID or manuscript DOI.
