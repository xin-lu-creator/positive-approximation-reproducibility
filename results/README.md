# Results

- `frozen/`: byte-frozen submission-version exact certificate output.
- `generated/`: outputs from fresh verification runs.

`run_all.py` requires the regenerated certificate CSV to be byte-identical to the frozen reference.
