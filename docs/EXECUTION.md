# Execution

Fast check:

```bash
python run_demo.py
```

Complete check:

```bash
python run_all.py
```

The complete check writes only to `results/generated/` and does not overwrite the frozen reference.
