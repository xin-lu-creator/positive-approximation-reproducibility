# Installation

Recommended:

```bash
python -m venv .venv
```

Activate the environment, then install:

```bash
python -m pip install -r requirements.txt
```

Alternatively use the supplied `environment.yml` with Conda/Mamba.
