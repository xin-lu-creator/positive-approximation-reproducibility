# Troubleshooting

- If `numpy` or `sympy` cannot be imported, install `requirements.txt`.
- Run commands from the repository root.
- Do not modify `results/frozen/counterexample_chord_bounds.csv`; it is the submission-version reference.
- A byte-comparison failure means the regenerated certificate file is not identical to the frozen reference and should be investigated before release or citation.
