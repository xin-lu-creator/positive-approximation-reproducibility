# Hardware information

The verification tasks are small deterministic CPU calculations. No GPU is required.

Reference package validation was performed in the OpenAI execution environment used for release preparation. The scripts are not hardware-specific.
